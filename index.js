// 백준 문제일 경우 제출 코드에 다음 코드 주석 해제
// const rawInput = require("fs").readFileSync(0, "utf-8").toString();

function solution(input) {
  // 풀이 코드 작성
  let answer = 0;
  return answer;
}

// 제출 시 삭제 필수
export { solution };

// 백준 문제일 경우 제출 코드에 다음 코드 추가
/*
function main() {
  const rawInput = ``; // 예제 입력 그대로 복사. 제출 시 삭제 필수
  const parsedInput = rawInput.trim();

  let output = "";
  output += solution(parsedInput);
  console.log(output.trim());
}

main();

// 제출 시 삭제 필수
export { main };
*/
